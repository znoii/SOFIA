<UserControl x:Class="SportSofia.Windows.AdminControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SportSofia.Windows"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TabControl Grid.Row="1">
            <TabItem Header="Inventory">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Orientation="Horizontal">
                        <Button Content="Add Inventory" Margin="5" Click="AddInventory_Click"/>
                        <Button Content="Edit Inventory" Margin="5" Click="EditInventory_Click"/>
                        <Button Content="Delete Inventory" Margin="5" Click="DeleteInventory_Click"/>
                    </StackPanel>

                    <ListView x:Name="InventoryListView" Grid.Row="1" Margin="5" SelectionMode="Single">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="Number" DisplayMemberBinding="{Binding InventoryNumber}" Width="100"/>
                                <GridViewColumn Header="Name" DisplayMemberBinding="{Binding Name}" Width="150"/>
                                <GridViewColumn Header="Type" DisplayMemberBinding="{Binding Type}" Width="100"/>
                                <GridViewColumn Header="State" DisplayMemberBinding="{Binding State}" Width="100"/>
                                <GridViewColumn Header="User" DisplayMemberBinding="{Binding Reader.Name}" Width="150"/>
                            </GridView>
                        </ListView.View>
                    </ListView>
                </Grid>
            </TabItem>

            <TabItem Header="Users">
                <ListView x:Name="UsersListView" Margin="5">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="Login" DisplayMemberBinding="{Binding Login}" Width="100"/>
                            <GridViewColumn Header="Name" DisplayMemberBinding="{Binding Name}" Width="100"/>
                            <GridViewColumn Header="Surname" DisplayMemberBinding="{Binding Surname}" Width="100"/>
                            <GridViewColumn Header="Role" DisplayMemberBinding="{Binding Role.Name}" Width="100"/>
                        </GridView>
                    </ListView.View>
                </ListView>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>