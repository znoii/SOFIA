<Window x:Class="SportSofia.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SportSofia.Windows"
        mc:Ignorable="d"
        Title="SportSofia" 
        WindowStartupLocation="CenterScreen"
        Height="600" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Right">
            <TextBlock Text="Аккаунт: " Margin="5"/>
            <TextBlock Text="{Binding CurrentUser.Name}" Margin="5"/>
            <TextBlock Text="{Binding CurrentUser.Surname}" Margin="5,5,10,5"/>
            <Button Content="Выйти?" Margin="5" Padding="10,2" Click="LogoutButton_Click"/>
        </StackPanel>

        <Frame x:Name="MainFrame" Grid.Row="1" NavigationUIVisibility="Hidden"/>

        <StatusBar Grid.Row="2">
            <StatusBarItem Content="SportSofia"/>
        </StatusBar>
    </Grid>
</Window>